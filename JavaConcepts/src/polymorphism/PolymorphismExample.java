package polymorphism;

public class PolymorphismExample {
	
	
	/*
	 * Overriding /Dynamic binding / late binding /Run time polymorphism
	 * 
	 * different classes (with inheritance applied) we have methods with same
	 * signature
	 * 
	 * covariant - the return types are className and there is parent - child relationship between those classes.
	 * 
	 */
	

}
